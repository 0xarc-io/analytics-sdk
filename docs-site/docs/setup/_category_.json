{
  "label": "4. Setup",
  "position": 4,
  "link": {
    "type": "generated-index",
    "description": "Before we can utilise the SDK, we must first instantiate it either via the React Provider or the `.init` method on the SDK instance."
  }
}
